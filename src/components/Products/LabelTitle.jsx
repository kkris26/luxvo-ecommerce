import React from "react";

const LabelTitle = ({ children }) => {
  return (
    <p className="text-center tracking-wider font-extralight text-sm text-black/50">
      {children}
    </p>
  );
};

export default LabelTitle;
